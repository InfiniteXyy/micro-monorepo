{
  "name": "@xyz/service-search",
  "type": "module",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . && tsc --noEmit",
    "start": "node dist/server.js",
    "test": "vitest --run",
    "dev:test": "vitest"
  },
  "dependencies": {
    "@trpc/server": "catalog:",
    "@xyz/lib-service-core": "workspace:*",
    "zod": "catalog:"
  }
}
